export class ShoppingCartItem {
    constructor(public id: string, public name: string, public imgurl: string, public artType: string, public artSize: string,
                public price: number, public quantity: number) { }
}
